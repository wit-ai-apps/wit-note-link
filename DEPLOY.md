# DEPLOY 手順（固定）- wit-note-link

## 前提
- 配布URLは必ず ?b=BUILD_ID を付ける
- 検証だけ &debug=1（必要時のみ）

## 配布URLの作り方
- 通常配布：<URL>?b=<BUILD_ID>
- 検証用：<URL>?b=<BUILD_ID>&debug=1

## 配布後チェック（目視）
- UIが変わっていない（最優先）
- A4実寸／横ズレ無し／スクロール等の挙動が崩れていない
